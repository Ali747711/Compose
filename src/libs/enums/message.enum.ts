export enum ConversationStatus {
  OPEN = "OPEN",
  CLOSED = "CLOSED",
}

export enum MessageSenderType {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum MessageStatus {
  SENT = "SENT",
  READ = "READ",
}
