<nav class="fixed left-0 top-0 h-full w-64 bg-white border-r border-gray-200 transform -translate-x-full lg:translate-x-0 transition-transform duration-300 z-40" id="sidebar">
  <!-- Brand -->
  <div class="h-20 flex items-center px-6 border-b border-gray-100">
    <div class="flex items-center gap-3">
      <div class="w-10 h-10 bg-main/20 rounded-xl flex items-center justify-center">
        <i class="bi bi-cup-hot-fill text-xl text-main-dull"></i>
      </div>
      <div>
        <h2 class="text-lg font-bold text-main-text">Compose</h2>
        <span class="text-xs text-gray-400">Admin Panel</span>
      </div>
    </div>
  </div>

  <!-- Navigation -->
  <ul class="py-4 px-3 space-y-1">
    <li>
      <a href="/admin/dashboard"
         class="flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-200 <%= activePage === 'dashboard' ? 'bg-main/20 text-main-text font-semibold border-l-4 border-main' : 'text-gray-600 hover:bg-main/10 hover:text-main-text' %>">
        <i class="bi bi-speedometer2 text-lg"></i>
        <span>Dashboard</span>
      </a>
    </li>
    <li>
      <a href="/admin/products"
         class="flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-200 <%= activePage === 'products' ? 'bg-main/20 text-main-text font-semibold border-l-4 border-main' : 'text-gray-600 hover:bg-main/10 hover:text-main-text' %>">
        <i class="bi bi-cup-straw text-lg"></i>
        <span>Products</span>
      </a>
    </li>
    <li>
      <a href="/admin/orders"
         class="flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-200 <%= activePage === 'orders' ? 'bg-main/20 text-main-text font-semibold border-l-4 border-main' : 'text-gray-600 hover:bg-main/10 hover:text-main-text' %>">
        <i class="bi bi-cart3 text-lg"></i>
        <span>Orders</span>
      </a>
    </li>
    <li>
      <a href="/admin/users"
         class="flex items-center gap-3 px-4 py-3 rounded-xl transition-all duration-200 <%= activePage === 'users' ? 'bg-main/20 text-main-text font-semibold border-l-4 border-main' : 'text-gray-600 hover:bg-main/10 hover:text-main-text' %>">
        <i class="bi bi-people text-lg"></i>
        <span>Users</span>
      </a>
    </li>
  </ul>

  <!-- Footer -->
  <div class="absolute bottom-0 left-0 right-0 p-4 border-t border-gray-100">
    <a href="/admin/logout"
       class="flex items-center gap-3 px-4 py-3 text-red-500 hover:bg-red-50 rounded-xl transition-all duration-200">
      <i class="bi bi-box-arrow-left text-lg"></i>
      <span>Logout</span>
    </a>
  </div>
</nav>

<!-- Sidebar Overlay for Mobile -->
<div class="fixed inset-0 bg-black/50 z-30 lg:hidden hidden" id="sidebarOverlay"></div>
