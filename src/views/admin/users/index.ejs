<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Compose Coffee Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            main: '#fdd22c',
            'main-dull': '#e3b609',
            'main-duller': '#ee9b00',
            'main-text': '#2e2726',
          },
          fontFamily: {
            sans: ['Quicksand', 'sans-serif'],
          },
        }
      }
    }
  </script>
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/admin/css/admin.css">
</head>
<body class="bg-gray-50 font-sans">
  <div class="flex min-h-screen">
    <%- include('../partials/sidebar', { activePage }) %>

    <div class="flex-1 lg:ml-64">
      <%- include('../partials/header', { user, title }) %>

      <main class="p-6">
        <!-- Alerts -->
        <% if (success) { %>
          <div class="mb-6 p-4 bg-green-50 border border-green-200 rounded-xl flex items-center justify-between">
            <p class="text-green-600 text-sm font-medium">User status updated successfully!</p>
            <button onclick="this.parentElement.remove()" class="text-green-400 hover:text-green-600">
              <i class="bi bi-x-lg"></i>
            </button>
          </div>
        <% } %>
        <% if (error) { %>
          <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-xl flex items-center justify-between">
            <p class="text-red-600 text-sm font-medium"><%= error %></p>
            <button onclick="this.parentElement.remove()" class="text-red-400 hover:text-red-600">
              <i class="bi bi-x-lg"></i>
            </button>
          </div>
        <% } %>

        <!-- Filters -->
        <div class="bg-white rounded-2xl border border-gray-200 shadow-sm mb-6">
          <div class="p-6">
            <form method="GET" class="grid grid-cols-1 md:grid-cols-12 gap-4">
              <div class="md:col-span-3">
                <select name="status" class="w-full px-4 py-3 border border-gray-300 rounded-xl text-gray-700 focus:outline-none focus:border-main focus:ring-2 focus:ring-main/20 transition-all">
                  <option value="">All Status</option>
                  <% statuses.filter(s => s !== 'DELETE').forEach(s => { %>
                    <option value="<%= s %>" <%= filters.status === s ? 'selected' : '' %>><%= s %></option>
                  <% }) %>
                </select>
              </div>
              <div class="md:col-span-7">
                <input type="text" name="search" placeholder="Search by name, email, or phone..."
                       value="<%= filters.search || '' %>"
                       class="w-full px-4 py-3 border border-gray-300 rounded-xl text-gray-700 placeholder-gray-400 focus:outline-none focus:border-main focus:ring-2 focus:ring-main/20 transition-all">
              </div>
              <div class="md:col-span-2">
                <button type="submit" class="w-full py-3 px-4 bg-main text-main-text font-bold rounded-xl border-2 border-main-dull hover:bg-main-dull transition-all duration-200 flex items-center justify-center gap-2">
                  <i class="bi bi-search"></i> Filter
                </button>
              </div>
            </form>
          </div>
        </div>

        <!-- Users Table -->
        <div class="bg-white rounded-2xl border border-gray-200 shadow-sm">
          <div class="px-6 py-4 border-b border-gray-100 flex items-center gap-2">
            <i class="bi bi-people text-main-dull"></i>
            <h3 class="font-semibold text-main-text">Users (<%= users.total || 0 %>)</h3>
          </div>

          <div class="overflow-x-auto">
            <table class="w-full">
              <thead class="bg-gray-50">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">User</th>
                  <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Email</th>
                  <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Phone</th>
                  <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Points</th>
                  <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Joined</th>
                  <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Status</th>
                  <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Actions</th>
                </tr>
              </thead>
              <tbody class="divide-y divide-gray-100">
                <% if (users.data && users.data.length > 0) { %>
                  <% users.data.forEach(u => { %>
                    <tr class="hover:bg-gray-50 transition-colors">
                      <td class="px-6 py-4">
                        <div class="flex items-center gap-3">
                          <img src="<%= u.userImage || '/admin/images/default-avatar.png' %>"
                               alt=""
                               class="w-10 h-10 rounded-full object-cover border border-gray-200">
                          <span class="font-medium text-main-text"><%= u.userNick %></span>
                        </div>
                      </td>
                      <td class="px-6 py-4 text-sm text-gray-600"><%= u.userEmail || 'N/A' %></td>
                      <td class="px-6 py-4 text-sm text-gray-600"><%= u.userPhone %></td>
                      <td class="px-6 py-4">
                        <span class="inline-flex px-2 py-1 rounded-lg text-xs font-medium bg-main/20 text-main-dull">
                          <%= u.userPoints %> pts
                        </span>
                      </td>
                      <td class="px-6 py-4 text-sm text-gray-600"><%= new Date(u.createdAt).toLocaleDateString() %></td>
                      <td class="px-6 py-4">
                        <select class="status-select px-3 py-1.5 text-xs border border-gray-200 rounded-lg focus:outline-none focus:border-main focus:ring-1 focus:ring-main/20"
                                data-id="<%= u._id %>"
                                data-type="user">
                          <% statuses.filter(s => s !== 'DELETE').forEach(s => { %>
                            <option value="<%= s %>" <%= u.userStatus === s ? 'selected' : '' %>><%= s %></option>
                          <% }) %>
                        </select>
                      </td>
                      <td class="px-6 py-4">
                        <a href="/admin/users/<%= u._id %>"
                           class="inline-flex items-center gap-1 px-3 py-1.5 text-sm text-blue-600 hover:bg-blue-50 rounded-lg transition-colors">
                          <i class="bi bi-eye"></i> View
                        </a>
                      </td>
                    </tr>
                  <% }) %>
                <% } else { %>
                  <tr>
                    <td colspan="7" class="px-6 py-12 text-center text-gray-400">
                      <i class="bi bi-inbox text-3xl block mb-2"></i>
                      No users found
                    </td>
                  </tr>
                <% } %>
              </tbody>
            </table>
          </div>

          <% if (users.pages > 1) { %>
            <div class="px-6 py-4 border-t border-gray-100">
              <nav class="flex justify-center">
                <div class="flex items-center gap-1">
                  <% for (let i = 1; i <= users.pages; i++) { %>
                    <a href="?page=<%= i %>&status=<%= filters.status || '' %>&search=<%= filters.search || '' %>"
                       class="px-4 py-2 rounded-lg text-sm font-medium transition-colors <%= users.currentPage === i ? 'bg-main text-main-text' : 'text-gray-600 hover:bg-gray-100' %>">
                      <%= i %>
                    </a>
                  <% } %>
                </div>
              </nav>
            </div>
          <% } %>
        </div>
      </main>

      <%- include('../partials/footer') %>
    </div>
  </div>

  <script src="/admin/js/admin.js"></script>
</body>
</html>
